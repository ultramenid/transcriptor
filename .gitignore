# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# ffmpeg/ffprobe sidecar binaries are fetched per-platform in CI from
# eugeneware/ffmpeg-static — never committed (they're large + per-target).
src-tauri/binaries/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.claude/
.vscode/
# Keep CI workflows tracked (needed to build Windows binaries for sharing).
!.github/workflows/
